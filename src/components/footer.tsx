import Image from 'next/image';
import type { FC } from 'react';

export const Footer: FC = () => {
  return <footer className='mt-auto flex h-12 w-full items-center justify-center border-t'>footer</footer>;
};
